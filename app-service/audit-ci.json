{
  "high": true,
  "//": "These advisories are for nedb (an abandoned transitive dependency of matrix-appservice-bridge). It is low risk because the bridge only uses nedb for local internal state files (room/user stores), meaning the database isn't exposed to arbitrary untrusted external payloads that could trigger prototype pollution. We must wait for the bridge maintainers to migrate away. Underscore vulnerabilities were patched via package.json overrides.",
  "allowlist": ["GHSA-339j-hqgx-qrrx"]
}
